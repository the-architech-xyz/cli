/**
 * Core Genome Transformer Interface
 */
import { GenomeTransformer, TransformationContext, ValidationResult, ProcessedGenome } from './types.js';
export declare abstract class BaseGenomeTransformer implements GenomeTransformer {
    abstract readonly name: string;
    abstract readonly priority: number;
    abstract readonly description: string;
    abstract transform(genome: ProcessedGenome, context: TransformationContext): Promise<ProcessedGenome>;
    canTransform(genome: ProcessedGenome): boolean;
    validate(genome: ProcessedGenome): Promise<ValidationResult>;
    protected logStep(context: TransformationContext, message: string, meta?: any): void;
    protected logError(context: TransformationContext, message: string, error: Error): void;
    protected logSuccess(context: TransformationContext, message: string, meta?: any): void;
}
//# sourceMappingURL=genome-transformer.d.ts.map