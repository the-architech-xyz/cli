/**
 * Unified Genome Transformation Service
 *
 * Orchestrates all genome transformations in a single pipeline
 */
import { Genome } from '@thearchitech.xyz/types';
import { TransformationConfig, TransformationResult, GenomeTransformer } from './types.js';
export declare class GenomeTransformationService {
    private transformers;
    private config;
    private initialized;
    private moduleIndex?;
    constructor(config: TransformationConfig);
    /**
     * Transform a genome through the complete pipeline
     */
    transform(genome: Genome): Promise<TransformationResult>;
    /**
     * Add a custom transformer
     */
    addTransformer(transformer: GenomeTransformer): void;
    /**
     * Remove a transformer by name
     */
    removeTransformer(name: string): void;
    /**
     * Get all transformers
     */
    getTransformers(): GenomeTransformer[];
    /**
     * Get transformer by name
     */
    getTransformer(name: string): GenomeTransformer | undefined;
    /**
     * Initialize default transformers
     */
    private initializeDefaultTransformers;
    /**
     * Check if genome is capability-driven
     */
    private isCapabilityDrivenGenome;
    private loadModuleIndex;
    private getManifestCandidates;
    private getCapabilityManifestCandidates;
    private resolveConfigPath;
    private extractManifestEntries;
    private normalizeManifestEntry;
    private serializeModuleIndex;
    private applyModuleMetadata;
    private resolvePath;
    private mergeTemplates;
    private extractParameterDefaults;
    private resolveModuleEntry;
}
//# sourceMappingURL=transformation-service.d.ts.map