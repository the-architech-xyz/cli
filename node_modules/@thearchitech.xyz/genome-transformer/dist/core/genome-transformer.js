/**
 * Core Genome Transformer Interface
 */
export class BaseGenomeTransformer {
    canTransform(genome) {
        return true; // Override in subclasses for specific conditions
    }
    async validate(genome) {
        return {
            valid: true,
            errors: [],
            warnings: []
        };
    }
    logStep(context, message, meta) {
        context.logger.info(`🔄 [${this.name}] ${message}`, meta);
    }
    logError(context, message, error) {
        context.logger.error(`❌ [${this.name}] ${message}`, {
            error: error.message,
            stack: error.stack
        });
    }
    logSuccess(context, message, meta) {
        context.logger.info(`✅ [${this.name}] ${message}`, meta);
    }
}
//# sourceMappingURL=genome-transformer.js.map