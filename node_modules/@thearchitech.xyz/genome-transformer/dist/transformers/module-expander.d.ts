/**
 * Module Expansion Transformer
 *
 * Expands feature modules and adds required dependencies
 */
import { BaseGenomeTransformer } from '../core/genome-transformer.js';
import { TransformationContext, ProcessedGenome } from '../core/types.js';
export declare class ModuleExpander extends BaseGenomeTransformer {
    readonly name = "Module Expansion";
    readonly priority = 2;
    readonly description = "Expands feature modules and adds required dependencies";
    canTransform(genome: ProcessedGenome): boolean;
    transform(genome: ProcessedGenome, context: TransformationContext): Promise<ProcessedGenome>;
    private expandFeatureModules;
    private addRequiredAdapters;
    private addMarketplaceDefaults;
    private getRequiredFeatureModules;
    private getRequiredAdapters;
    private getModuleIndex;
    private extractFeatureName;
}
//# sourceMappingURL=module-expander.d.ts.map