import { BaseGenomeTransformer } from '../core/genome-transformer.js';
import { ProcessedGenome, TransformationContext } from '../core/types.js';
export declare class ManifestFeatureResolver extends BaseGenomeTransformer {
    readonly name = "Manifest Feature Resolver";
    readonly priority = 2;
    readonly description = "Resolves abstract feature modules using manifests to concrete implementations";
    canTransform(genome: ProcessedGenome): boolean;
    transform(genome: ProcessedGenome, context: TransformationContext): Promise<ProcessedGenome>;
    private needsManifestResolution;
    private extractFeatureId;
    private loadFeatureManifest;
    private pickImplementations;
}
//# sourceMappingURL=manifest-feature-resolver.d.ts.map