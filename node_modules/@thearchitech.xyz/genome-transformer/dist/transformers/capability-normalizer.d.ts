/**
 * Capability Normalization Transformer
 *
 * Converts capability-driven genomes to traditional module-based genomes
 */
import { BaseGenomeTransformer } from '../core/genome-transformer.js';
import { TransformationContext, ProcessedGenome } from '../core/types.js';
export declare class CapabilityNormalizer extends BaseGenomeTransformer {
    readonly name = "Capability Normalization";
    readonly priority = 1;
    readonly description = "Converts capability declarations to executable modules";
    canTransform(genome: ProcessedGenome): boolean;
    transform(genome: ProcessedGenome, context: TransformationContext): Promise<ProcessedGenome>;
    private resolveCapabilities;
    /**
     * Auto-resolve framework module from project.framework
     */
    private resolveFrameworkModule;
    private resolveAdapter;
    private resolveTechStackLayer;
    private resolveFrontendLayer;
    private resolveBackendLayer;
    private resolveDatabaseLayer;
    private isCapabilityDrivenGenome;
}
//# sourceMappingURL=capability-normalizer.d.ts.map