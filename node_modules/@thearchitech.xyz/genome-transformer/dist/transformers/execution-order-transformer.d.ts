import { BaseGenomeTransformer } from '../core/genome-transformer.js';
import { ProcessedGenome, TransformationContext } from '../core/types.js';
/**
 * ExecutionOrderTransformer
 *
 * Provides a stable module ordering so the CLI can execute modules without its own planner/graph.
 * Simple deterministic order: adapters -> connectors -> features. Keeps relative order within groups.
 */
export declare class ExecutionOrderTransformer extends BaseGenomeTransformer {
    readonly name = "Execution Order";
    readonly priority = 98;
    readonly description = "Orders modules deterministically for execution (adapters -> connectors -> features)";
    canTransform(_genome: ProcessedGenome): boolean;
    transform(genome: ProcessedGenome, context: TransformationContext): Promise<ProcessedGenome>;
}
//# sourceMappingURL=execution-order-transformer.d.ts.map