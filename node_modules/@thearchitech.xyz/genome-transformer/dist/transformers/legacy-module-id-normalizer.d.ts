import { BaseGenomeTransformer } from '../core/genome-transformer.js';
import { ProcessedGenome, TransformationContext } from '../core/types.js';
/**
 * LegacyModuleIdNormalizer
 *
 * Rewrites legacy module IDs to the current marketplace structure and removes duplicates.
 * Examples:
 *  - framework/nextjs -> adapters/framework/nextjs
 *  - ui/shadcn-ui -> adapters/ui/shadcn-ui
 */
export declare class LegacyModuleIdNormalizer extends BaseGenomeTransformer {
    readonly name = "Legacy Module ID Normalizer";
    readonly priority = 0;
    readonly description = "Normalizes legacy module IDs to new adapter paths and de-duplicates.";
    canTransform(_genome: ProcessedGenome): boolean;
    transform(genome: ProcessedGenome, context: TransformationContext): Promise<ProcessedGenome>;
}
//# sourceMappingURL=legacy-module-id-normalizer.d.ts.map