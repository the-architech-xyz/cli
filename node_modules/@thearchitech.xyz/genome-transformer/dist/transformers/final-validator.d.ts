/**
 * Final Validation Transformer
 *
 * Performs final validation and preparation for execution
 */
import { BaseGenomeTransformer } from '../core/genome-transformer.js';
import { TransformationContext, ValidationResult, ProcessedGenome } from '../core/types.js';
export declare class FinalValidator extends BaseGenomeTransformer {
    readonly name = "Final Validation";
    readonly priority = 5;
    readonly description = "Performs final validation and preparation for execution";
    canTransform(genome: ProcessedGenome): boolean;
    transform(genome: ProcessedGenome, context: TransformationContext): Promise<ProcessedGenome>;
    validate(genome: ProcessedGenome): Promise<ValidationResult>;
    private validateModules;
    private detectConflicts;
    private prepareForExecution;
    private sortModulesByExecutionOrder;
    private extractFeatureName;
}
//# sourceMappingURL=final-validator.d.ts.map