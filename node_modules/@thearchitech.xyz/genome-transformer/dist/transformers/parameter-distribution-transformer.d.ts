/**
 * Parameter Distribution Transformer
 *
 * Transforms capability parameters into module-specific parameters
 * with validation to ensure modules can handle the distributed parameters.
 */
import { BaseGenomeTransformer } from '../core/genome-transformer.js';
import { TransformationContext, ProcessedGenome } from '../core/types.js';
export declare class ParameterDistributionTransformer extends BaseGenomeTransformer {
    readonly name = "Parameter Distribution";
    readonly priority = 3;
    readonly description = "Distributes capability parameters to modules with validation";
    private distributionService;
    constructor();
    canTransform(genome: ProcessedGenome): boolean;
    transform(genome: ProcessedGenome, context: TransformationContext): Promise<ProcessedGenome>;
    validate(genome: ProcessedGenome): Promise<{
        valid: boolean;
        errors: string[];
        warnings: string[];
    }>;
}
//# sourceMappingURL=parameter-distribution-transformer.d.ts.map