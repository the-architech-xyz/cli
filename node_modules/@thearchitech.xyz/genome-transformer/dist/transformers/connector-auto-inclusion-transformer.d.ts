/**
 * Connector Auto-Inclusion Transformer
 *
 * Automatically adds connectors that bridge technologies selected by the user.
 * Analyzes resolved modules and identifies technology gaps that need bridging.
 */
import { BaseGenomeTransformer } from '../core/genome-transformer.js';
import { TransformationContext, ProcessedGenome } from '../core/types.js';
export interface TechnologyBridge {
    from: string;
    to: string;
    connectorId: string;
    required: boolean;
}
export declare class ConnectorAutoInclusionTransformer extends BaseGenomeTransformer {
    readonly name = "Connector Auto-Inclusion";
    readonly priority = 4;
    readonly description = "Automatically adds connectors that bridge selected technologies";
    private technologyBridges;
    constructor();
    canTransform(genome: ProcessedGenome): boolean;
    transform(genome: ProcessedGenome, context: TransformationContext): Promise<ProcessedGenome>;
    validate(genome: ProcessedGenome): Promise<{
        valid: boolean;
        errors: string[];
        warnings: string[];
    }>;
    /**
     * Initialize technology bridges
     */
    private initializeTechnologyBridges;
    /**
     * Find required connectors for a module
     */
    private findRequiredConnectors;
    /**
     * Find dynamic connectors based on module patterns
     */
    private findDynamicConnectors;
    /**
     * Search for connectors in the marketplace
     */
    private searchConnectorsInMarketplace;
    /**
     * Create a connector module
     */
    private createConnectorModule;
    /**
     * Extract default parameters from connector schema
     */
    private extractDefaultParameters;
}
//# sourceMappingURL=connector-auto-inclusion-transformer.d.ts.map