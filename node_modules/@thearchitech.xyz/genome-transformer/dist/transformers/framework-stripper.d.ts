import { BaseGenomeTransformer } from '../core/genome-transformer.js';
import { ProcessedGenome, TransformationContext } from '../core/types.js';
/**
 * Removes framework adapter modules from the module list.
 * Frameworks are handled via project.apps and should not execute as modules.
 */
export declare class FrameworkStripper extends BaseGenomeTransformer {
    name: string;
    description: string;
    priority: number;
    canTransform(genome: ProcessedGenome): boolean;
    transform(genome: ProcessedGenome, context: TransformationContext): Promise<ProcessedGenome>;
}
//# sourceMappingURL=framework-stripper.d.ts.map