/**
 * Tech-Stack Resolution Transformer
 *
 * Detects and applies tech-stack overrides (tRPC, SDK overrides)
 */
import { BaseGenomeTransformer } from '../core/genome-transformer.js';
import { TransformationContext, ProcessedGenome } from '../core/types.js';
export declare class TechStackResolver extends BaseGenomeTransformer {
    readonly name = "Tech-Stack Resolution";
    readonly priority = 3;
    readonly description = "Applies tech-stack overrides based on detected technologies";
    canTransform(genome: ProcessedGenome): boolean;
    transform(genome: ProcessedGenome, context: TransformationContext): Promise<ProcessedGenome>;
    private detectTRPCUsage;
    private applyTRPCOverrides;
    private detectSDKOverrides;
    private applySDKOverrides;
    private extractFeatureName;
}
//# sourceMappingURL=tech-stack-resolver.d.ts.map