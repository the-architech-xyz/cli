/**
 * Dynamic Questioner - TEMPORARILY DISABLED
 * 
 * This component needs to be rewritten for the new adapter system.
 * The old plugin system has been replaced with tech-agnostic adapters.
 */

export class DynamicQuestioner {
  constructor() {
    // Temporarily disabled
  }
  
  async generateQuestions(): Promise<any[]> {
    return [];
  }
} 